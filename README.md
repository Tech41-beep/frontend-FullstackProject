# VisualInternship

A React + Vite landing page built with Tailwind CSS and Bootstrap. It includes a header with Company, About Us, Contact Us, and Login entry points, plus a login form and supporting content sections.

## Scripts

- `npm run dev` - start the development server
- `npm run build` - create a production build
- `npm run preview` - preview the production build locally
